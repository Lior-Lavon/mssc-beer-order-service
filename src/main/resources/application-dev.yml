#Database configuration for MySql Dev
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/beer_order_dev?useUnicode=true&characterEncoding=UTF-8&serverTimezone=UTC
    username: beer_order_user
    password: password
    platform: mysql
    hikari:
      maximum-pool-size: 5
  jpa:
    hibernate:
      ddl-auto: create-drop
    database-platform: org.hibernate.dialect.MySQL5Dialect
    database: mysql
    show-sql: false
